<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      {{#link-to 'index'}}<a class="navbar-brand">Brackety</a>{{/link-to}}
    </div>

    <ul class="nav navbar-nav navbar-right">
      {{#if session.isAuthenticated}}
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
            Logged in as Edgar Lee <span class="caret"></span>
          </a>
          <ul class="dropdown-menu" role="menu">
            <li>
              {{link-to 'Profile' 'accounts' session.user.account.username}}
            </li>
            <li><a href="#" {{action 'invalidateSession'}}>Log out</a></li>
          </ul>
        </li>
      {{else}}
        <li><a href="#" {{action 'authenticateSession'}}>Log in</a></li>
      {{/if}}
    </ul>
  </div>
</nav>

<div class="container">
  {{outlet}}
</div>
