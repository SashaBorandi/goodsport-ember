<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      {{link-to "Goodsport" "index" class="navbar-brand"}}
    </div>

    <ul class="nav navbar-nav navbar-right">
      {{#if session.isAuthenticated}}
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
            Logged in as Edgar Lee <span class="caret"></span>
          </a>
          <ul class="dropdown-menu" role="menu">
            <li>
              {{link-to "Profile" "account" session.user.account.username}}
            </li>
            <li class="divider"></li>
            <li>{{link-to "Settings" "settings"}}</li>
            <li><a href="#" {{action "invalidateSession"}}>Log out</a></li>
          </ul>
        </li>
      {{else}}
        <li><a href="#" {{action "authenticateSession"}}>Log in</a></li>
      {{/if}}
    </ul>
  </div>
</nav>
